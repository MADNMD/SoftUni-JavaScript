function cityRecord(city, population, treasury){

    const cityRecord = {
        name: city,
        population: population,
        treasury: treasury,
    }
    return cityRecord;
}
cityRecord('Tortuga',
7000,
15000);