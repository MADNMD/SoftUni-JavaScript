<main id="details">
    <section id="details-info">
        <div class="game-image">
            <img src="{{image}}">
        </div>

        <div class="game-info">
            <div class="game-text">
                <h1 id="name">{{name}}</h1>
                <h3 id="platform">Platform: {{platform}}</h3>
                <p id="price"><span>Price: ${{price}}</span></p>
                <p id="genre"><span>Genre: {{genre}}</span></p>
                <p id="description">{{description}}</p>
            </div>

            {{#if user}}
                <div class="product-btn"></div>
                    {{#if isOwner}}
                        <!--Only for registered user and author of the publication -->
                        <div class="author">
                            <a href="/game/{{_id}}/edit" class="btn-edit">Edit</a>
                            <a href="/game/{{_id}}/delete" class="btn-delete">Delete</a>
                        </div>
                    {{else}}  
                        {{#if isBought}}  
                            <!-- Logged in user who has already buy this game code-->
                            <p class="buy-message">You have already bought this game</p>
                        {{else}}
                            {{!-- Logged in user who has not yet buy this game code --}}
                            <a href="/game/{{_id}}/buy" class="btn-buy">Buy</a>
                        {{/if}}
                    {{/if}}
                </div>
            {{/if}}
        </div>
    </section>
</main>